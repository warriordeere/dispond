import { NextRequest } from "next/server";

export function GET(req: NextRequest) {
    //[TODO] fetch list (not via API!!) then filter results using the given dispatch type and return the files content!
}